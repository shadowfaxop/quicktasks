import Foundation

extension Notification.Name {
    /// Posted when the task list has been modified (add, remove, toggle, etc.).
    static let taskListUpdated = Notification.Name("taskListUpdated")
}
